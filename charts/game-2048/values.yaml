replicaCount: 3

image:
   repository: public.ecr.aws/l6m2t8p7/docker-2048
   tag: latest
   pullPolicy: IfNotPresent

namespace:
  name: game-2048

serviceAccount:
  enabled: false

service:
  port: 80
  targetPort: 80
  type: ClusterIP

sidecar:
   dashboards:
      enabled: true
